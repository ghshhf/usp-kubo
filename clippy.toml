[workspace.metadata.clippy]
# Enable all lint categories
cognitive-complexity-threshold = 30
too-many-arguments-threshold = 10

[lints.rust]
unsafe-code = "deny"

[lint双人合作]
doc-over-long-line-list = "warn"
missing-debug-implementations = "warn"
